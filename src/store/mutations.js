import {VIEW_COOM} from './types'

let mutations={ 

  // [VIEW_NAV]:(state,payload)=> state.bNav=payload,
  [VIEW_COOM]:(state,payload)=> state.bCoom=payload,
  // [VIEW_LOADING]:(state,payload)=> state.bLoading=payload,

};
export default mutations;
